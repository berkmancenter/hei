<li class="span5">
  <div class="thumbnail">
    <div class="caption">
      <h3><%= project.title %></h3>
      <% unless project.description.blank? %>
        <p><%= project.description %></p>
      <% end %>
      <p>
      <%= link_to('Info', projects_path(p), :class => 'btn btn-primary') %>
      </p>
    </div>
  </div>
</li>
