<div class="caption">
  <h3><%= project.title %></h3>

  <% if project.role.present? %>
    <div class="role"><%= project.role %></div>
  <% end %>

  <div><%= project.organization.nil? ? raw( '&nbsp;' ) : "@ #{project.organization.name}" %></div>
</div>

<div class='description'><%= project.description || raw( '&nbsp;' ) %></div>
